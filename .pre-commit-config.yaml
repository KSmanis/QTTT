default_install_hook_types:
  - commit-msg
  - pre-commit
exclude:
  glob: CHANGELOG.md
repos:
  # Meta
  - repo: meta
    hooks:
      - id: check-hooks-apply
        priority: &read-only 1000
      - id: check-useless-excludes
        priority: *read-only
  # Builtin
  - repo: builtin
    hooks:
      - id: check-executables-have-shebangs
        priority: *read-only
      - id: check-shebang-scripts-are-executable
        priority: *read-only
      - id: end-of-file-fixer
        priority: &write-serial null
      - id: trailing-whitespace
        priority: *write-serial
  # General
  - repo: https://github.com/renovatebot/pre-commit-hooks
    rev: 44.104.2
    hooks:
      - id: renovate-config-validator
        args: ["--strict"]
        priority: *read-only
  # Git
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
        priority: *read-only
  - repo: https://github.com/jorisroovers/gitlint
    rev: v0.19.1
    hooks:
      - id: gitlint
        priority: *read-only
      - id: gitlint-ci
  # GitHub Actions
  - repo: https://github.com/python-jsonschema/check-jsonschema
    rev: 0.38.2
    hooks:
      - id: check-github-workflows
        priority: *read-only
  - repo: https://github.com/rhysd/actionlint
    rev: v1.7.12
    hooks:
      - id: actionlint
        priority: *read-only
  - repo: https://github.com/zizmorcore/zizmor-pre-commit
    rev: v1.29.0
    hooks:
      - id: zizmor
        args: ["--fix", "--no-progress"]
        priority: &write-parallel 999
  # Java
  - repo: local
    hooks:
      - id: google-java-format
        name: google-java-format
        entry: mise exec -- google-java-format --aosp --skip-sorting-imports --replace
        language: system
        priority: *write-parallel
        types: ["java"]
  # JSON
  - repo: builtin
    hooks:
      - id: pretty-format-json
        args: ["--autofix", "--no-ensure-ascii"]
        priority: *write-parallel
      - id: check-json
        priority: *read-only
  # Markdown
  - repo: https://github.com/rvben/rumdl-pre-commit
    rev: v0.2.77
    hooks:
      - id: rumdl
        args: ["--fix"]
        priority: *write-parallel
  # Shell
  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.11.0.1
    hooks:
      - id: shellcheck
        priority: *read-only
  # TOML
  - repo: https://github.com/pappasam/toml-sort
    rev: v0.25.0
    hooks:
      - id: toml-sort-fix
        args: ["--all", "--in-place", "--trailing-comma-inline-array"]
        priority: *write-parallel
  - repo: builtin
    hooks:
      - id: check-toml
        priority: *read-only
  # YAML
  - repo: https://github.com/google/yamlfmt
    rev: v0.21.0
    hooks:
      - id: yamlfmt
        priority: *write-parallel
  - repo: https://github.com/adrienverge/yamllint
    rev: v1.38.0
    hooks:
      - id: yamllint
        priority: *read-only
