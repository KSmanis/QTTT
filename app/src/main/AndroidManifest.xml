<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.QTTT"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".OptionsActivity"
            android:label="@string/activity_options_title"
            android:parentActivityName=".MainActivity"
            android:launchMode="singleTop"/>
        <activity
            android:name=".SingleActivity"
            android:label="@string/activity_single_title"
            android:parentActivityName=".OptionsActivity"/>
        <activity
            android:name=".MultiActivity"
            android:label="@string/activity_multi_title"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".TutorialActivity"
            android:label="@string/activity_tutorial_title"
            android:parentActivityName=".MainActivity"/>
    </application>

</manifest>
